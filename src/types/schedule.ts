export interface ClassInfo {
    fId: string;
    day: number;
    start: number;
    end: number;
}

export interface ImageClassInfo {
    day: number;
    startTime: string;
    endTime: string;
    color: string;
}
